# Treat Gradle Wrapper and all JAR files as binary to avoid corruption on push/pull
*.jar binary

# Treat scripts as text with LF line endings
gradlew text eol=lf
*.sh text eol=lf

# Treat batch files as text with CRLF line endings
*.bat text eol=crlf
*.cmd text eol=crlf
